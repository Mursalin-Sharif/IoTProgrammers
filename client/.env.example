# Leave empty for same-origin production (Express serves the built site + /api).
# Local Vite dev can omit this file — defaults to http://localhost:5000
# VITE_API_URL=http://localhost:5000

# Production same-origin (Hostinger single Node app):
VITE_API_URL=
